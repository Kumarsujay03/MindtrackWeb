VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=

# Client-side Turso (PUBLIC at build time)
# These are embedded into the frontend bundle. Anyone can see the values in DevTools.
# Use only a read-only token here.
VITE_TURSO_DATABASE_URL=
VITE_TURSO_AUTH_TOKEN=

# Server-side only (Vercel Project Settings -> Environment Variables)
# Do NOT prefix with VITE_. The frontend must not see these.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
